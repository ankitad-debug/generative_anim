let x,y; 
function setup() {
  createCanvas(400, 400);
  x=0;
  y=400;
}

function draw() {
  background(220);
 
 /* //L to R
  fill(x,y,200,100);
  ellipse(x,200,x/2)
  x=(x+5)%400

  //R to L
  ellipse(y,200,100)
  y=(y-5)

 /* //ellipse that follows the mouse
  ellipse(mouseX,mouseY,100)

  //fill(mouseX,mouseY,50)
  
 
  //L to R
  fill(x,y,200,100);
  ellipse(x,200,20);
  x=(x+5);

  //R to L
  ellipse(y,200,100);
  y=(y-5);
  */

}
